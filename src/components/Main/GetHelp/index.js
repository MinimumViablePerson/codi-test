import React from 'react'
import styled from 'styled-components'

function GetHelp({ className, ...props }) {
  return (
    <div className={className} {...props}>
      <h1>Get Help</h1>
    </div>
  )
}

export default styled(GetHelp)``
